num length = 10+randomInt(10);
str setBlock = "ruin_block";
num random = randomInt(2);

if (random == 0) {
    for (num x = -length/2; x < length/2; x = x + 1) {
    structure(x,0,0,setBlock,"NONE");
    }
} else {
    for (num x = -length/2; x < length/2; x = x + 1) {
    structure(0,0,x,setBlock,"NONE");
    }
}

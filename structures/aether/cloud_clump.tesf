str cloudBlock = "minecraft:white_stained_glass";
str altCloudBlock = "minecraft:light_gray_stained_glass";
str placeBlock = cloudBlock;
for (num x = 0; x <= 1; x = x + 1) {
  for (num y = 0; y <= 1; y = y + 1) {
    for (num z = 0; z <= 1; z = z + 1) {
        placeBlock = cloudBlock;
        if (randomInt(2) == 0) placeBlock = altCloudBlock;
        block(x, y, z, placeBlock, false);
        }
    }  
}

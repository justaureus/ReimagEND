str setBlock = "minecraft:white_stained_glass";

for (num x = 0; x < 1; x = x + 1) {
  for (num y = 0; y < 1; y = y + 1) {
    for (num z = 0; z < 1; z = z + 1) {

        setBlock = "minecraft:white_stained_glass";
        if (randomInt(4) == 0) setBlock = "minecraft:pearlescent_froglight";
        block(x, y, z, setBlock, false);
        }
    }  
}